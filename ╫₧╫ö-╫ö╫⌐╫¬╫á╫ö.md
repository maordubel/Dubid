# DUBID — סבב 3 · דומיינים, לוגואים, הרשמה, ומסך ההרכב

**תאריך:** 25 באוגוסט 2026

לפרוס מעל `Desktop\Dubid` (מיזוג, לא החלפה).

```
npm install
npm run dev
```

**בדיקות:** 222/222 עוברות · `tsc --noEmit` נקי · רינדור אמיתי של
עץ הקומפוננטות ב-Node · כל המיגרציות + שני קובצי בדיקת SQL רצים
על PostgreSQL אמיתי, שוב ושוב ובכל סדר.

---

## 1. הדומיינים

מקור אמת אחד — `src/lib/growth.ts`:

```ts
export const DUBEL     = { site: 'https://www.dubelteam.com', … };
export const DUBID_URL = 'https://dubid.dubelteam.com';
export const OFFSIDES  = { url: 'https://offsides.dubelteam.com', … };
```

`offsidebets.dubel.team` הוסר מכל מקום — קוד, בדיקות, SQL, תיעוד.
הבדיקה ב-`tests/growth.test.ts` תתפוס אם מישהו יחזיר אותו.

⚠ אם הכתובת הישנה עדיין חיה — עדיף **redirect 301** ולא שני שמות
במקביל. אחרת קישורי שיתוף ישנים ימשיכו לנחות שם והשיוך יישבר.

★ שלושתם תחת אותו דומיין־אב, וזה לא רק אסתטי: זה מה שיאפשר
בעתיד סשן משותף דרך עוגייה על `.dubelteam.com` בלי להעביר אף
טוקן בכתובת.

---

## 2. הלוגואים

שניהם נגזרו מהמקור בסקריפט, ונשמרו ב-`brand/source/`.

**דובל טים** → `public/brand/dubel-team{,@128,@64}.png`.
`DubelCredit.tsx` כבר חיפש את הקובץ הזה וכל הזמן נפל לכתר מצויר,
כי הוא פשוט לא היה שם. עכשיו הוא שם. הוא **עגול במקור**, ולכן
הוסר ה-`rounded-full` שהיה עליו — עיגול על עיגול חותך את הקצה
של הציור.

**אופסיידס** → `public/brand/offsides{,@160}.png`, בבאנר.
עד עכשיו היה שם דגל־קו מצויר. סימן ממציא הוא פשרה סבירה כשאין
לוגו, והוא הופך למכשול ברגע שיש: הוא מלמד את המשתמש לזהות צורה
שלא תופיע באתר שאליו הוא לוחץ.

---

## 3. הרשמה

> **הכלל לא השתנה: אורח הוא אזרח מן המניין.** הוא משחק, מדורג,
> ומופיע בטבלה כמו כולם. שום מסך לא חוסם, ואין טופס בכניסה.

מה שנוסף הוא **ביטוח**, לא שער כניסה:

| דרך | מתי |
|---|---|
| אורח | ברירת מחדל. בלי טופס |
| קוד חד־פעמי | אותו אורח, מכשיר אחר |
| גוגל | לחיצה אחת |
| אימייל + סיסמה | טופס מקופל, נפתח למי שרוצה |
| חשבון אופסיידס | למי שכבר יש |

### ★ השדרוג מאורח לרשום הוא שינוי דגל, לא הגירה

אותה שורה במסד. `id` לא זז, ההרכבים לא זזים, הדירוג לא זז.
`is_guest` הופך ל-`false`.

זה חשוב יותר ממה שזה נשמע: מסלול הגירה שמעביר דאטה יכול להיכשל
באמצע ולהשאיר מישהו בלי היסטוריה. דגל לא יכול.

### ★ הדחיפה — מציעים רק למי שיש לו מה לאבד

`src/lib/nudge.ts`, פונקציה טהורה עם שש בדיקות:

```
isGuest  &&  לא סגר במחזור הזה  &&  (הגיש הרכב || פורסמו תוצאות)
```

אורח שנכנס עכשיו ועוד לא בחר שחקן לא מסכן כלום, והצעה באותו רגע
היא מס כניסה. אותה הצעה בדיוק, אחרי שהוא הגיש — מתארת סיכון
אמיתי. הסגירה (×) משתיקה למחזור שלם, לא עד רענון.

בלי מספרים מומצאים, בלי מודאל, שורה אחת.

---

## 4. ★★ באג במסמך שצירפת ★★

`DUBIDAUTHSYNC.md` §8 מתאר באג אמיתי ומשוחזר: התנגשות שמות
משתמש מפילה הרשמות שלמות.

**הפאץ' שהוא מציע נופל על אותו תרחיש בדיוק.**

הוא גוזר סיומת מ**ארבעה** תווים של ה-UUID — אותו אורך כמו
הגיבוי השבור שהוא בא לתקן. הבדיקה שלנו הריצה את שלושת המשתמשים
מהמסמך עצמו מול הפאץ׳:

```
info@alpha.com   4336d23f…  →  info_4336   ✓
info@beta.com    11111111…  →  info_1111   ✓
info@gamma.com   1111aaaa…  →  info_1111   ✗ duplicate key
```

זה אותו זוג UUID שהמסמך הביא כהוכחה לבאג. הפאץ' רק הזיז אותו
שלב אחד אחורה.

**אצלנו — שלוש דרגות, והשלישית לא יכולה להתנגש:**

```
1.  base                    פנוי → נשמר תו-בתו
2.  base_XXXXXXXX           שמונה תווים
3.  base_<32 תווים מלאים>   ה-UUID השלם = ייחודי בהגדרה
```

בלי לולאת "ספור עד שפנוי" (שהיא תחרות בין שתי הרשמות מקבילות).

**נמדד:** 400 הרשמות מעורבות — גוגל בלי שם, שמות תפוסים, עברית,
שם בן תו אחד, רווחים בלבד → **0 כשלונות, 0 כפילויות, 0 שמות
ריקים**, ושמות עבריים פנויים נשמרו תו-בתו:

```
אלוף · אלוף_D5598A59 · מלך_הקרנות · מלך_הקרנות_33541B08
```

⚠ **בצד אופסיידס — לבדוק את אורך הסיומת ב-`patch-v18.9.453`.**
אם היא ארבעה תווים, אותו באג ממתין שם.

### עוד שני דברים מהמסמך שיושמו

* `username.trim().length` **לפני** הבדיקה, לא אחריה. במסמך זה
  מסומן כבאג פתוח בשני הצדדים (`"  a"` עובר ומגיע כ-`"a"`).
* `username_available()` לפני השליחה — כדי שהתנגשות תהיה הערה
  ליד השדה, ולא `Database error saving new user` בסוף טופס מלא.

### ההנחה שלא מתקיימת

המסמך נכתב עבור **פרויקט Supabase אחד**. ההחלטה בפועל הייתה שני
פרויקטים, ולכן:

* אין `auth.users` משותף.
* גוגל בדוביד יוצרת משתמש **דוביד** — לא מזהה חשבון אופסיידס.
* לאופסיידס נדרש **לקוח OAuth נפרד**, כי ה-callback שלו מצביע
  לפרויקט אחר.
* מי שכבר יש לו חשבון שם צריך `link-offsides`, לא `signUp`.

הפירוט המלא של מה תקף ומה לא: **`docs/AUTH.md` §1.**

---

## 5. מסך ההרכב הנעול — נבנה מחדש

זה היה המסך היחיד במוצר שלא ציית לשפה שלו.

מסך הבחירה מצייר **מגרש** — דשא, סימונים, שחקנים בעמדות
הטקטיות, הכל נכנס בגובה אחד בלי גלילה. המסך הנעול צייר ארבע
שורות שטוחות ("שוער", "הגנה"…) על מלבן דשא, עם לוחיות שם מהפלטה
הישנה — **והוא נגלל**.

התוצאה: אותו הרכב נראה כמו שני דברים שונים לפני ואחרי ההגשה.
מי שבנה 4-3-3 וראה אותו על מגרש, לחץ "שמירה" וקיבל רשימה — וזה
קורא כאילו משהו **נלקח** ממנו ברגע ההגשה.

עכשיו: אותו `<Pitch>`, אותו `fit="height"`, אותה אנטומיית כרטיס
(סמל · שם · שורה שלישית), אותו סרגל CTA. שורת צ׳יפים למעלה
במקום הכרטיס הכחול. ההבדל היחיד בין המסכים הוא שכאן אי אפשר
לגעת — וזה נאמר בשקט, דרך היעדר הכפתורים.

**שני תיקונים בדרך:**

* `fill` ב-`AppShell` היה דולק רק במצב הבנייה, ולכן המסך הנעול
  נגלל. עכשיו שניהם עמודה בגובה קבוע.
* השורה השלישית בכרטיס מציגה את הקבוצה לפני הפרסום ואת הנקודות
  אחריו — **אותה משבצת**, ולכן ההרכב לא "קופץ" כשהתוצאות נכנסות.

---

## מה צריך ממך

1. **להריץ `db/10_accounts.sql`** על פרויקט דוביד (אחרי 09).
   `SELECT * FROM game.v_accounts_health;` → `duplicate_usernames = 0`.
2. **Authentication → Providers:** לוודא ש-**Anonymous sign-ins
   דלוק** (בלעדיו אין אורחים), ולהפעיל Google + Email.
3. **Site URL** → `https://dubid.dubelteam.com`, ולהוסיף
   `http://localhost:5173` ל-Redirect URLs.
4. **לקוח OAuth של גוגל** — `docs/AUTH.md` §3. שימו לב:
   **לקוח נפרד לכל פרויקט.**
5. **להחליט על אישור מייל** — `docs/AUTH.md` §4. למחזור 2 אני
   ממליץ **כבוי**: זה חיכוך מיותר לקבוצת ניסוי, והקוד תומך בשני
   המצבים בלי שינוי.
6. **redirect 301** מהדומיין הישן של אופסיידס, אם הוא עדיין חי.

## פתוח, ודורש החלטה

1. **דאטת כדורגל מאופסיידס** — הצד שלנו מוכן, חסר `pg_dump
   --schema-only --schema=public` שלו.
2. **מתי דוביד מעניק פרס** — הגשר בנוי בשני הצדדים, אף אחד לא
   קורא לו.
3. **"שכחתי סיסמה"** — לא נבנה בכוונה: הוא דורש תבנית מייל
   מוגדרת, ובלעדיה הכפתור שולח לשום מקום. עדיף בלי כפתור מאשר
   כפתור ששקר.
4. **מיזוג היסטוריות** כשאורח עם הגשות מקשר חשבון שכבר מקושר.
   כרגע הישן מנצח.
