# DUBID — סבב 8 · השער של האדמין, מהשורש

**תאריך:** 25 באוגוסט 2026

---

## מה קרה, ולמה זו טעות שלי

היו **שני מקורות אמת** ל"אני אדמין":

```
1.  דגל ב-sessionStorage   ← נדלק כשה-hash של הסיסמה תאם בדפדפן
2.  game.users.is_admin     ← המסד
```

שניים כאלה תמיד מסכימים — עד שלא. וזה מה שקרה אצלך: הסיסמה
הייתה נכונה, השער המקומי הדליק את הדגל, **הקריאה לשרת נכשלה**,
ונכנסת. מאותו רגע:

* רענון הכניס אותך ישר פנימה, **בלי לבקש סיסמה**
* ובפנים — שום פעולה לא עבדה, כי במסד לא היית אדמין

שני התסמינים שדיווחת עליהם, אותו שורש אחד.

★ הוספתי את השער המקומי כדי "לחסוך הלוך-ושוב לשרת על שגיאת
הקלדה". חיסכון של 200 מילישניות תמורת מצב שבו המסך פתוח
וההרשאה לא קיימת. עסקה גרועה.

---

## התיקון — מקור אמת אחד

```
ready  ⇔  game.is_admin() החזירה true
```

ולא "וגם", ולא "או".

* **אין דגל מקומי. אין sessionStorage.** נמחקו לגמרי
  (`ADMIN_PIN_HASH`, `tryAdminLogin`, `isAdminSession`,
  `adminLogout`, מונה הניסיונות).
* **הסיסמה נבדקת רק בשרת.** קריאה אחת, `game.claim_admin`.
* **כישלון = נעול.** "לא הצלחתי לבדוק" אינו "כן".
* **מצב ביניים לא מציג כלום.** מסך שמהבהב "אין הרשאה" ואז
  נפתח נראה שבור.

המכונה יושבת ב-`src/lib/adminGate.ts` — טהורה, בלי React ובלי
דפדפן, עם 5 בדיקות. אחת מהן עוברת על **כל** צירופי הקלט
ומוודאת שאין מסלול שמגיע ל-`ready` בלי אישור המסד.

★ הבאנר הישן ("אין לך הרשאת ניהול במסד" עם ה-SQL) — **נמחק.**
הוא לא יכול יותר לקרות: אי אפשר להיות בפנים בלי הרשאה.

---

## איך זה נבדק הפעם

לא בקריאת קוד. **רינדור אמיתי** של לוח הניהול, עם שרת שנכשל:

```
✓ מציג מצב בדיקה ולא מסך פתוח
✓ אין כפתור פרסום
✓ אין לשונית סגלים
✓ אין הזנת תוצאות
✓ אין כפתור יציאה של הלוח
ADMIN_GATE_OK
```

ובמסד, המסלול המלא על מסד נקי:

```
✓ משתמש חדש — לא אדמין
✓ הסיסמה hapoelTA14! מעניקה הרשאה
✓ סגלים, מחירים ותוצאות — עובדים
✓ יציאה מכבה את ההרשאה
```

`RUN-ALL` פעמיים: **0 שגיאות**. ארבע חבילות SQL: PASS.

---

## דלת חירום

אם המסד עדיין לא מכיר את `claim_admin`, שום סיסמה לא תעזור —
ובלי מוצא המסך הופך לקיר.

לכן במצב הזה המסך מציג **את המזהה שלך ואת השורה המדויקת**
שמעניקה לך הרשאה ידנית, עם כפתור העתקה:

```sql
UPDATE game.users SET is_admin = TRUE WHERE id = '<המזהה שלך>';
```

---

## מה לעשות עכשיו

1. לפרוס את הקבצים
2. `npm run build` + פריסה
3. `/admin` → הסיסמה `hapoelTA14!`

★ **אם `db/RUN-ALL.sql` כבר רץ אצלך — זה יעבוד מיד.**
בצילום שלך ארבע החוליות הראשונות היו ירוקות, כלומר המסד תקין.
רק ההרשאה חסרה, וזה בדיוק מה שהסיסמה עושה עכשיו.

★ אם עדיין לא רץ — המסך יגיד "המסד לא הוגדר עדיין" ויציג את
דלת החירום.
