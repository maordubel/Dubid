# DUBID — סבב 5 · אדמין בכתובת אחת, ו"אין חיבור לשרת"

**תאריך:** 25 באוגוסט 2026

---

# 🔑 הכניסה לאדמין

```
https://dubid.dubelteam.com/admin
```

בפיתוח: `http://localhost:5173/admin`

**סיסמה:** `hapoelTA14!`

הכתובת הישנה `#admin` ממשיכה לעבוד.

---

## 1. ★ "אין חיבור לשרת" — נפתר

**ההודעה עצמה הייתה הבאג.** היא נכונה בכל מקרה ולא עוזרת באף
אחד, ובפועל כמעט אף פעם אין בעיית רשת: המיגרציה לא רצה,
הסכימות לא חשופות, או כניסת אורחים כבויה. שלוש בעיות, שלושה
תיקונים במקומות שונים לגמרי — ואותה הודעה חסרת תועלת.

PostgREST כבר מבדיל ביניהן, הוא רק לא עושה את זה בעברית:

| קוד | מה זה באמת | מה יוצג עכשיו |
|---|---|---|
| `PGRST202` / `42883` | הפונקציה לא קיימת | "המסד לא הוגדר עדיין — חסרות מיגרציות" |
| `PGRST106` | הסכימה לא חשופה | "Settings → API → Exposed schemas" |
| `anonymous_provider_disabled` | אורחים כבויים | "Authentication → Providers → Anonymous sign-ins" |
| `401` / `403` / `42501` | GRANT / RLS | "אין הרשאה לקרוא מהמסד" |
| `TypeError` | באמת אין רשת | "אין חיבור לאינטרנט" |

תקלת הגדרה מוצגת **בצהוב ולא באדום**: אדום אומר "משהו נשבר",
וכאן משהו פשוט עוד לא הוגדר. המשחק ממשיך לעבוד מקומית בינתיים,
והפס אומר את זה.

### ולמה זה קורה לך עכשיו

**כמעט בוודאות: `db/09` ואילך עוד לא רצו** — נתקעת על שגיאת
הסכימה בסבב הקודם ולא המשכת. הפס החדש יגיד לך את זה במילים.

📍 **הדרך המהירה:** להיכנס ל-`/admin` → לשונית **"בדיקת מערכת"**.

## 2. בדיקת מערכת

לשונית שלישית בלוח הניהול. חמש חוליות, **בסדר התלות**:

```
1  זהות (כניסת אורח)        →  Anonymous sign-ins
2  סכימה ומיגרציות          →  db/09..12  /  Exposed schemas
3  תוצאות ונתוני מחזור      →  db/09
4  שעון השרת                →  db/09 §3b
5  הרשאת ניהול              →  להקליד את הסיסמה
```

★ חוליה שתלויה באחת שנפלה מסומנת "—" ו**לא נבדקת**. אחרת היית
רודף אחרי חמש שגיאות שכולן נובעות מאותו מקור.

כל שורה שנופלת אומרת את התיקון המדויק, בלוח הבקרה של Supabase.

## 3. ★ סיסמה אחת במקום שני שלבים

**לפני:** הקוד במסך פתח אותו. ההרשאה לשמור דרשה
`UPDATE game.users SET is_admin = TRUE` ידני ב-SQL Editor. מי
שעשה רק את הראשון קיבל **לוח מלא שבו כל לחיצה נכשלת** — וזה
נראה כמו באג, לא כמו שלב שנשכח.

**עכשיו:** הסיסמה נשלחת ל-`game.claim_admin` בשרת. נכונה → אתה
גם נכנס וגם מקבל הרשאה. **אין SQL Editor.**

★ **והאבטחה נשארה.** הבדיקה בשרת היא שקובעת: מי שיערוך את הקוד
בדפדפן יפתח את המסך ולא ישמור כלום, כי כל פונקציית אדמין במסד
בודקת `game.is_admin()` בשורה הראשונה.

★ **חמישה ניסיונות → נעילה לחמש דקות**, כולל מול הסיסמה הנכונה.
נעילה שנפרצת בסיסמה הנכונה היא חסרת ערך — תוקף שמנחש היה פשוט
ממשיך.

★ **"יציאה" מכבה גם את הדגל במסד.** אחרת כל מכשיר שהוקלדה בו
הסיסמה נשאר אדמין לנצח — כולל טלפון שהושאל לרגע.

★ הסיסמה **לא נשמרת** — רק SHA-256 שלה, וטבלת הסודות אינה
נגישה מהדפדפן (נבדק).

### להחליף סיסמה

```sql
UPDATE game.admin_secrets
   SET secret_hash = encode(digest('חדשה','sha256'),'hex'),
       rotated_at = now()
 WHERE id = 'primary';
```

⚠ לעדכן גם את `ADMIN_PIN_HASH` ב-`src/lib/store.ts`.

## 4. Google Analytics

`G-F75FRV6MMR` כבר מותקן ב-`index.html` — בדיוק כפי ששלחת, עם
`preconnect` שחוסך DNS+TLS מלאים בשנייה שבה המסך נטען.

## 5. ★★ באג שהבדיקה החדשה תפסה ★★

`game.admin_squads()` פנתה ל-`tm.short_name`. **העמודה נקראת
`short_code`.** כל טעינה של מסך הסגלים הייתה נכשלת.

הבדיקה הקודמת (`db/tests/03` §7) בדקה רק שהפונקציה **נדחית**
למי שאינו אדמין — ולכן היא מעולם לא רצה בהצלחה, והבאג שרד.

> **בדיקה שמוודאת רק שמשהו נכשל אינה בדיקה שהוא עובד.**

תוקן, ו-`db/tests/03` הורחב כדי שהפונקציה באמת תרוץ: 14 קבוצות,
קיצורים, ומזהים בשפת הקליינט.

---

## מה צריך ממך

1. **להריץ `db/12_admin_access.sql`** — חדש.
2. **להריץ מחדש `db/11_arena_and_squads.sql`** — מכיל את תיקון
   `short_code`. אידמפוטנטי.
3. להיכנס ל-**`/admin`** עם `hapoelTA14!`.
4. **"בדיקת מערכת"** → כל מה שאדום אומר לך בדיוק מה חסר.

**בדיקות:** 222 יחידה · `tsc` נקי · רינדור אמיתי · **12
מיגרציות + 4 חבילות SQL** על PostgreSQL אמיתי, פעמיים ברצף.

התקלות האחרות שהזכרת — מחכות לך כשתגיע אליהן.
