# DUBID — סבב 7 · הכל בהדבקה אחת

**תאריך:** 25 באוגוסט 2026

📄 **`docs/GO-LIVE.md`** — ארבעה שלבים, חמש דקות, ואתה משחק עם חברים.

---

## 1. ★ `db/RUN-ALL.sql` — קובץ אחד

אחד־עשר הקבצים אוחדו לאחד. **הדבקה אחת ל-SQL Editor, Run, נגמר.**

★ **למה זה נבנה:** "להריץ אחד־עשר קבצים לפי הסדר" היא הוראה
שנכשלת. מספיק לדלג על אחד או להריץ שניים הפוך כדי לקבל שגיאה
שנראית כמו באג במוצר. **שתי התקלות האחרונות שלך היו בדיוק זה.**

קובץ אחד = אין סדר לטעות בו.

נבנה בסקריפט (`scripts/build-all-sql.mjs`) ולא ידנית, כך שהוא לא
יכול להתיישן ביחס למקורות. בדקתי: הרצה על מסד ריק, ואז שוב על
אותו מסד — **0 שגיאות**, וארבע חבילות הבדיקה עוברות אחריו.

---

## 2. ★★ הבאג שהשאיר אותך בלי דרך להתקדם ★★

בצילום שלך: **"השרת לא ענה. בדקו חיבור ונסו שוב."**

`claimAdmin` מיפתה **כל** שגיאה שאינה "המיגרציה חסרה" ל-`NETWORK`.
כלומר: סכימה לא חשופה, אורחים כבויים, בעיית הרשאות — כולן הוצגו
כ"בעיית רשת". שלחתי אותך לבדוק את האינטרנט כשהבעיה הייתה מתג
בלוח הבקרה.

**וגרוע מזה:** בדיקת המערכת שבניתי בשבילך ישבה **בתוך** לוח
הניהול — מעבר לדלת שלא נפתחת.

### תוקן

* `claimAdmin` מעביר את הקוד המדויק. המסך אומר "הסכימות לא
  חשופות" או "כניסת אורחים כבויה" — כלומר את התיקון עצמו.
* **בדיקת המערכת זמינה ממסך הכניסה**, בלי להיכנס. קישור קבוע,
  ולא רק אחרי כישלון.
* תקלת תשתית **פותחת אותה אוטומטית** — אתה ממילא הולך לשם.

---

## 3. מה שאני לא יכול לעשות, ולמה

ניסיתי להתחבר לפרויקט שלך מכאן כדי להריץ הכל בעצמי. **הסביבה
שלי חוסמת את `supabase.co`** — אין לי דרך להגיע.

לכן הכיוון היה להוריד את מה שנשאר עליך למינימום: **הדבקה אחת,
שני מתגים, build, וכניסה.** כל השאר כבר עובד ונבדק.

---

## מה צריך ממך — בסדר הזה

```
1.  להדביק את db/RUN-ALL.sql ב-SQL Editor  →  Run
2.  Exposed schemas:  public, core, game, shared
    Anonymous sign-ins:  ON
3.  npm run build  +  פריסה
4.  /admin  עם  hapoelTA14!
```

ואז:

```sql
UPDATE game.gameweeks SET status = 'open' WHERE code = 'gw-2';
```

ולשלוח לחברים את `https://dubid.dubelteam.com` — **בלי הרשמה.**

---

**בדיקות:** 222 יחידה · `tsc` נקי · רינדור אמיתי · `RUN-ALL`
פעמיים על PostgreSQL אמיתי + 4 חבילות SQL אחריו.
