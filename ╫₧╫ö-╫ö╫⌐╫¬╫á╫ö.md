# DUBID — סבב 6 · שתי התקלות

**תאריך:** 25 באוגוסט 2026

שתיהן שוחזרו אצלי, תוקנו, ונבדקו.

---

## 1. `42P16: cannot drop columns from view`

### מה קרה

PostgreSQL לא מרשה ל-`CREATE OR REPLACE VIEW` **לשנות** את
רשימת העמודות — רק להוסיף בסוף.

`db/10` יוצר את `game.v_accounts_health` עם 6 עמודות.
`db/11` מרחיב אותו ל-8.
ואז — הרצה חוזרת של `db/10` מנסה לחזור ל-6, ונופלת.

★ **וזה בדיוק מה שיקרה לכל אחד.** כתבתי שהקבצים אידמפוטנטיים
ושאפשר להריץ אותם שוב — ואז בניתי שניים מהם כך שהסדר כן משנה.
זו סתירה שלי, לא טעות שלך.

### התיקון

כל תצוגה בכל המיגרציות עברה מ-

```sql
CREATE OR REPLACE VIEW game.v_x AS ...
```

ל-

```sql
DROP VIEW IF EXISTS game.v_x;
CREATE VIEW game.v_x AS ...
```

**עכשיו הסדר באמת לא משנה.** בדקתי: מעבר מלא, ואז 11 → 10 → 09
→ 03 → 12 → 11 שוב. הכל עובר.

⚠ שבעה קבצים עודכנו — לפרוס את כולם.

---

## 2. 404 על `/admin`

### מה קרה

`vercel.json` כן הכיל `rewrites: /(.*) → /index.html`, והוא לא
נכנס לתוקף. יש כמה סיבות אפשריות שקשה להבחין ביניהן מבחוץ:
Root Directory שמוגדר אחרת בפרויקט, framework preset שדורס, או
פריסה שלא קלטה את הקובץ.

★ **הטעות שלי הייתה להסתמך על הגדרת שרת בשביל כתובת.**

### התיקון: `/admin` הוא עכשיו קובץ אמיתי

```
public/admin.html          ← מוגש כש-cleanUrls דלוק
public/admin/index.html    ← מוגש בשרתים שמצפים לתיקייה
```

`Vite` מעתיק את `public/` לשורש ה-build, ולכן `/admin` נענה
**מהדיסק** — בלי rewrite, בלי הגדרה, ובכל שרת סטטי שקיים.
הקובץ מפנה מיד ל-`/#admin`.

עלות: קפיצה אחת של כמה עשרות מילישניות, עם ספינר ממותג כדי
שלא יהיה הבזק לבן.

`vercel.json` עודכן גם הוא — כגיבוי, לא כתלות.

---

# 🔑 אחרי הפריסה

```
https://dubid.dubelteam.com/admin
```

**סיסמה:** `hapoelTA14!`

ואז → לשונית **"בדיקת מערכת"**, שתגיד לך בדיוק מה עוד חסר.

## מה להריץ

```
db/09_live_mvp.sql
db/10_accounts.sql
db/11_arena_and_squads.sql
db/12_admin_access.sql
```

בכל סדר, כמה פעמים שרוצים. **עכשיו זה נכון.**

**בדיקות:** 222 יחידה · `tsc` נקי · רינדור אמיתי · 12 מיגרציות
+ 4 חבילות SQL על PostgreSQL, כולל בסדר שהפיל אותך.
