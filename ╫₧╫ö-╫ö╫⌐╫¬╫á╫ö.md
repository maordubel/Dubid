# DUBID — סבב הלוגו החדש · חלק א׳

**תאריך:** 24 באוגוסט 2026

התיקייה הזו היא הפרויקט המלא. אפשר להחליף בה את `Desktop\Dubid`
כמו שהוא — אין קבצים שנמחקו, רק קבצים שהתעדכנו ונוספו.

```
npm install
npm run dev
```

---

## 1. ★ למה האפליקציה לא עלתה

`src/App.tsx` — `useMemo` בשם `growthCtx` קרא ל-`entryFull`
ול-`entryFive` **עשרים שורות לפני** שהן הוצהרו.

ב-JavaScript, `const` בתוך אזור מת (TDZ) לא מחזיר `undefined` —
הוא זורק `ReferenceError`. ה-`useMemo` רץ בזמן הרינדור, כלומר
הרינדור הראשון של האפליקציה קרס. מסך לבן, תמיד, בכל מכשיר.

**תוקן.** הבלוק הועבר למעלה, עם הערה שמסבירה שאסור להזיז אותו בחזרה.

עוד שלוש שגיאות טיפוס תוקנו (משתנים ומאפיינים לא בשימוש שחסמו
את `tsc`).

**בדיקות:** 216/216 עוברות · `tsc --noEmit` נקי · רינדור אמיתי של
עץ הקומפוננטות ב-Node עובר.

---

## 2. הלוגו

המקור שסופק נשמר ב-`brand/source/dubid-logo-source.png`.
**אף נכס לא נערך ידנית** — הכל נגזר ממנו על ידי סקריפט:

```
node scripts/design/build-logo.mjs
```

הסקריפט מחלץ מסכת אלפא לפי כהות הפיקסל (הרקע הקרם **אינו** חלק
מהלוגו), ואז צובע את הקו. מכאן:

| קובץ | לְמה |
|---|---|
| `public/brand/dubid-logo-{gold,cream,ink}.png` | הלוגו המלא, שקוף, בשלושה צבעי קו |
| `public/brand/dubid-logo-*@360.png` | אותו דבר, קל פי חמישה, לשימוש קטן |
| `public/brand/dubid-mark-*.png` | הדיוקן בלבד, ריבועי — לכותרות ולאווטאר |
| `public/icons/*` | כל אייקוני ה-PWA, זהב על לילה |
| `public/favicon.svg`, `favicon-32/64` | פאביקון |

שני פרטים ששווה לדעת:

* **`logo-outline`** — ארבעה `drop-shadow` כהים בארבעה כיוונים.
  הקו של הלוגו דק, ומעל תמונה או גרדיאנט הוא נבלע. זה מחזיר לו
  הפרדה בלי לצייר מסגרת. `<Logo outline />`.
* **חיזוק אלפא מתחת ל-96px** — בהקטנה ל-32 הקו נופל לחמישית
  פיקסל וה-resampler הופך אותו לכתם חום. בלי החיזוק, הפאביקון
  היה כתם.

`src/components/Logo.tsx` הוא המקום היחיד שיודע איפה הקבצים.

---

## 3. השפה החזותית

הלוגו החדש הוא קו זהב על שחור. זו לא החלפת תמונה — זו החלפת
**צבע ראשי**. הכתום שהוביל את הממשק התחרה עם הזהב.

הדרך הזולה הייתה מצוא־והחלף ב-40 קבצים. במקום זה שונו
**הערכים של הטוקנים** ב-`tailwind.config.ts`, ולכן כל מסך במוצר
עבר לשפה החדשה בבת אחת — בלי רגע ביניים שבו חצי מהמסכים כתומים.
`toto` נשאר קיים ככינוי היסטורי ומצביע על אותו זהב.

* **פלטה** — שחור חם (`night`), זהב (`gold`), קרם (`chalk`).
  הכהים חמים ולא ניטרליים: זהב על אפור־כחלחל נראה ירקרק.
* **`tex-wood`** — משטח העץ הכהה. שלוש שכבות CSS, בלי תמונת רקע
  ובלי בקשת רשת.
* **`edge-gold`** — הקצה הזהוב שגורם לכרטיס להיראות מוטבע.

### הלובי — נבנה מחדש

ארבע קומות, בדיוק לפי הרפרנס: זהות → לוגו גיבור → פס מדדים →
פעולה. כרטיסי המצבים הפכו לשורות ברוחב מלא (בחצי רוחב הכותרת
נשברת והמשפט נחתך), נוספה רצועת "המשחקים הקרובים", ופס המדדים
מציג מקף ולא אפס כשאין עדיין נתון — "0 נקודות" ו"אין תוצאות" הם
שני דברים שונים.

**מה לא נלקח מהרפרנס, ובכוונה:** LVL, פעמון התראות, "רצף 7".
אין לנו את הנתונים האלה, ומדד שמראה מספר מומצא גרוע ממדד שלא קיים.

### ניווט

אייקונים מצוירים (`src/components/NavIcons.tsx`) במקום גליפים
כמו `◆` — אלה מרונדרים בפונט של מערכת ההפעלה ונראים אחרת בכל
מכשיר. הפריט האמצעי, **ההרכב**, מורם לעיגול זהב: זו הפעולה שכל
המוצר משרת.

---

## 4. Offsides

השם באנגלית היה כתוב בשלוש צורות, ואחת מהן פשוט שגויה
(`OFFSIDE BETS` בתמונת הפרסומת). נוסף בלוק `OFFSIDES`
ב-`src/lib/growth.ts` — מקור אמת אחד לשם, לשם בעברית, לדומיין.

⚠ **הדומיין `offsidebets.dubel.team` נשאר.** אם השם החדש אמור
להחליף גם אותו — תגיד, וזו שורה אחת.

---

## 5. ניקוי פנימי

`src/lib/leaderboard.ts` — צינור הדירוג (ניקוד → נדירות →
שוברי שוויון) יצא מתוך `Leaderboard.tsx` לקובץ טהור אחד. עכשיו
הלובי יכול להציג "המקום שלי" מאותו מקור בדיוק. לפני זה, כל מסך
שרצה מקום היה חייב לשכפל את שלושת השלבים — או לוותר. הלובי ויתר.

---

## מה בחלק ב׳ (בעבודה עכשיו)

שכבת שני ה-Supabase: זהות משותפת עם אופסיידס, כניסה עם קוד חד
פעמי, מעבר מ-localStorage ל-Supabase כמקור אמת, נעילה בצד שרת,
אדמין → תוצאות → פרסום → ניקוד → דירוג משותף.
