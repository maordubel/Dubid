/**
 * lib/pressPalette.ts — הפלטה של עמוד הספורט.
 *
 * ═══════════════════════════════════════════════════════════════
 * ★ למה קובץ, ולא שני עותקים של אותם שישה צבעים
 * ═══════════════════════════════════════════════════════════════
 *
 * הכרטיס בסטורי (`lib/revealCard.ts`) והמגרש בבחירת ההרכב
 * (`components/Pitch.tsx`) מציירים **את אותו דבר** בשני מנועים
 * שונים: אחד על קנבס, אחד ב-CSS ו-SVG.
 *
 * אילו הצבעים היו כתובים פעמיים, יום אחד מישהו היה מכהה את
 * הדשא בכרטיס ושוכח את המגרש — ואז מי שבנה הרכב וקיבל גזיר
 * עיתון היה רואה שני ירוקים שונים ומסיק, בצדק, שאלה שני
 * מוצרים. וזה בדיוק החיבור שהכרטיס אמור ליצור.
 *
 * שום בדיקה לא קוראת תמונות. קובץ אחד היא ההגנה היחידה.
 *
 * ═══════════════════════════════════════════════════════════════
 * ★ מאיפה הצבעים
 * ═══════════════════════════════════════════════════════════════
 *
 * מדפוס עיתון ולא ממסך: נייר שהצהיב, דיו שחור־חום (לא שחור
 * טהור — שחור טהור על נייר לא קיים), אדום רווי אחד, ודשא
 * שהודפס בשתי מסננות ירוקות ונקודות.
 *
 * הקובץ טהור: אין DOM, אין רשת. רק ערכים.
 */

export const PRESS = {
  /** נייר עיתון מצהיב. */
  paper: '#EFE7D5',
  paperDeep: '#E2D8C2',
  /** תיבות מקרא ופתקים על הנייר — לבן־נייר, לא לבן. */
  card: '#F6F3EA',
  /** דיו. שחור־חום, כי שחור טהור לא יוצא ממכונת דפוס. */
  ink: '#12100E',
  /** אדום הכותרות. */
  red: '#D93B3B',
  redDeep: '#B22A2A',
  /** דשא מודפס — שתי מסננות. */
  grass: '#8FBE63',
  grassDark: '#7DAE52',
  /** נקודות ההלפטון על הדשא. */
  dot: '#3F6B2C',
  /** קווי המגרש. לא לבן טהור — סיד על דשא. */
  line: '#F7F4EC',
  /** הילת הקריאות מאחורי שם על הדשא. */
  halo: '#EFF3E6',
  /** טקסט משני על הדשא — ירוק־דיו, לא אפור. */
  onGrass: '#2C3A25',
  /** רשת השער. שקוף למחצה, אחרת הוא נקרא כמלבן לבן. */
  net: 'rgba(232,234,217,.72)',
  /** חוטי הרשת. אפור־ירקרק, לא שחור — הרשת לא מצוירת בדיו. */
  netLine: '#9AA39A',
  /** צהוב הסימון — עיגולי המספרים על המגרש. */
  mark: '#F5C518',
} as const;

export type PressColor = keyof typeof PRESS;

/* =================================================================== */
/* מהדורת הלילה                                                        */
/* =================================================================== */

/**
 * ★ אותו עיתון, מודפס על נייר כהה.
 *
 * הלובי הוא המסך הראשון, ושם המוצר חייב להישאר לילה — זו הזהות.
 * אבל הוא גם חייב להיות **אותו עיתון** כמו הכרטיס והמגרש, אחרת
 * המשתמש רואה שני מוצרים.
 *
 * הפתרון: היפוך של אותה מערכת. הדיו הבהיר במקום השחור, הקווים
 * בזהב עמום במקום בדיו, והתצלום הופך לנגטיב — נקודות הלפטון
 * בהירות על כהה.
 *
 * ★ למה `paper` כאן אינו שחור
 *
 * `#100D0A` ולא `#000`. שחור טהור נקרא כמו **מסך**; נייר כהה
 * נקרא כמו נייר. ההפרש הזה הוא שלוש יחידות, והוא כל ההבדל.
 */
export const NIGHT_PRESS = {
  /** הנייר. כהה וחם, לא שחור מסך. */
  paper: '#100D0A',
  paperDeep: '#0B0907',
  /** משטח תיבה — הבדל עדין מהנייר, לא קופסה אפורה. */
  card: 'rgba(216,178,92,.035)',
  /** הדיו. שמנת, לא לבן — לבן טהור על כהה נשרף בעיניים. */
  ink: '#EDE4CE',
  inkDim: '#BFB49A',
  inkFaint: '#8A8069',
  /** הקווים. זהב עמום; קו לבן היה חותך את הדף לשניים. */
  rule: 'rgba(216,178,92,.42)',
  ruleStrong: 'rgba(216,178,92,.72)',
  gold: '#D8B25C',
  goldLight: '#F0D693',
  goldDeep: '#A9822F',
  /** אדום הכותרות, בגרסה שמחזיקה על כהה. */
  red: '#C4342F',
  redLight: '#E0574F',
  /** הדשא בתצלום המוסרק — כהה, כי הנקודות הן שמאירות אותו. */
  grass: '#2B3026',
  grassDark: '#1A2116',
} as const;

/**
 * ★ אי־התאמת לוחות הדפוס.
 *
 * בהדפסת צבע זולה של פעם הלוחות לא התיישרו בדיוק, וכל כותרת
 * קיבלה שפה אדומה מצד אחד וכחלחלה מהשני. זה הפרט הקטן שהמוח
 * מזהה כ"ישן" בלי לדעת למה — והוא **חייב** להיות קטן: 0.7
 * פיקסל. שניים כבר נראים כמו באג ברינדור.
 */
export const MISREGISTER =
  '.7px 0 0 rgba(196,52,47,.55), -.7px 0 0 rgba(80,150,190,.30)';
