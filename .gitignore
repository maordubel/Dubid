# ---------------------------------------------------------------------
#  לריפו הזה לא היה .gitignore בכלל.
#
#  ★ למה זה מסוכן ולא רק לא-מסודר: המסירה נעשית בהעלאת קבצים
#    בדפדפן, ו-`node_modules` הוא עשרות אלפי קבצים. העלאה אחת
#    בהיסח הדעת מציפה את הריפו ואי אפשר לנקות אותה בקליק.
# ---------------------------------------------------------------------

node_modules/
dist/
.vite/
*.tsbuildinfo

# ★ תוצר בנייה של scripts/sync-edge-shared.mjs.
#   רק של הפונקציה הזו — `dubid-ingest` שטוחה, והקבצים שלה
#   בשורש הפונקציה כן נשמרים בגיט (הם מה שמעלים ל-Dashboard).
supabase/functions/dubid-score-gameweek/_lib/

.env
.env.*
!.env.example

.DS_Store
Thumbs.db
*.log
