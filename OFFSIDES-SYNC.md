# אופסיידס ↔ דוביד · הסנכרון

**למי זה נכתב:** לעצמי, בפעם הבאה שאני נכנס לפרויקט אופסיידס.
כל מה שצריך כדי לחבר את שני המוצרים בלי לגזור מסקנות מחדש.

**עודכן:** אוגוסט 2026

> ⚠ **הגרסה הקודמת של המסמך הזה בטלה.** היא תיארה **פרויקט
> Supabase אחד** לשני המוצרים, עם `auth.users` משותף. ההחלטה
> השתנתה: לכל מוצר פרויקט משלו. אל תריצו את
> `db/08_offsides_coexistence.sql` — הוא נכתב לארכיטקטורה ההיא.

---

## 0. תקציר בעשר שניות

```
DUBID     afxpjfxwpdjvlmuoawda   eu-central-1   חדש
OFFSIDES  pqdzqpettxuuyngxbpxn   eu-west-1      חי, עם משתמשים
```

שני מסדים. שני `auth.users`. **אופסיידס הוא ספק הזהות**; דוביד
מחזיק מיפוי ומנפיק סשן משלו. הגבול בין המוצרים עובר בשרת, לא
בקליינט.

מה שאופסיידס צריך לעשות: מיגרציה אחת (`db/offsides/01_dubid_bridge.sql`)
ולהפעיל Email OTP. זהו.

---

## 1. למה זה לא פרויקט אחד

התכנון הקודם היה נכון טכנית: schemas נועדו בדיוק לזה. הוא נזנח
כי **אופסיידס חי**. יש לו משתמשים, היסטוריה, גיבויים, cron jobs,
webhooks ו-Storage. הגירה של כל זה בשביל נוחות ארכיטקטונית היא
סיכון שאין סיבה לקחת.

המחיר, וצריך לומר אותו בפירוש: **אין "אותו משתמש" ברמת המסד.**
כל תכנון שמניח שיש — טועה.

---

## 2. ★ איך "אותו חשבון" עובד בכל זאת

```
   דוביד (דפדפן)                אופסיידס              דוביד (שרת)
        │                           │                       │
   1. מקליד מייל ─────────────────► │                       │
                                    │ signInWithOtp
                                    │ shouldCreateUser=false
   2. ◄──── קוד למייל ──────────────┤                       │
   3. מקליד קוד ──────────────────► │                       │
                                    │ verifyOtp
   4. ◄──── access_token ───────────┤                       │
   5. שולח את הטוקן ──────────────────────────────────────► │
                                    │ ◄── GET /auth/v1/user ┤  6. אימות
                                    ├──── user.id ─────────►│
                                                            │  7. מיפוי
   8. ◄──────────── סשן של דוביד ───────────────────────────┤
```

### שלוש נקודות שאסור לפספס

**`shouldCreateUser: false` (שלב 1).** בלעדיו, מי שאין לו חשבון
אופסיידס נרשם בטעות מתוך דוביד. עם זה, הוא פשוט לא מקבל קוד —
וזו הודעת השגיאה הנכונה. ההבטחה למשתמש היא "אותו חשבון", לא
"עוד הרשמה".

**האימות בשלב 6 הוא כל האבטחה.** הדפדפן שולח טוקן וטוען שהוא
שלו; דפדפן יכול לשלוח כל דבר. רק צד שרת יכול לשאול את אופסיידס
"הטוקן הזה באמת שלך?". אין דרך קצרה יותר.

**למי שייכת ההיסטוריה (שלב 7).** אם חשבון האופסיידס עוד לא
קושר — הוא נצמד למשתמש האורח הנוכחי, וכל מה שהאורח בנה נשמר.
אם הוא כבר מקושר למשתמש דוביד אחר — **אותו** משתמש מנצח, כי בו
ההיסטוריה האמיתית. אורח בן שלוש דקות לא מנצח חודש של משחק.

מיזוג של שתי היסטוריות לא נעשה בכוונה: הוא דורש להחליט מה קורה
כששני הצדדים הגישו לאותו מחזור, וזו החלטת מוצר.

### מה אופסיידס צריך לעשות בשביל זה

```
Authentication → Providers → Email → Enable Email OTP
```

**זה הכל.** אין קוד, אין טבלה, אין endpoint. `GET /auth/v1/user`
כבר קיים בכל פרויקט Supabase.

---

## 3. המיגרציה — `db/offsides/01_dubid_bridge.sql`

אידמפוטנטית, additive, בטוחה על מסד חי. לא נוגעת בהימורים,
בזירות או ביתרות.

| מה | למה |
|---|---|
| עמודות שיוך על `profiles` | לדעת מי הגיע מדוביד ומאיזה רגע |
| `public.dubid_rewards` | פרסים נכנסים |
| `pending_dubid_rewards()` · `claim_dubid_reward(id)` | שתי הפונקציות שהקוד קורא |
| בדיקת `server_now()` | ראו §5 |

```sql
SELECT * FROM public.dubid_bridge_health;
```

---

## 4. גשר הפרסים — החוזה

**דוביד דוחף. אופסיידס קורא מטבלה של עצמו.**

הכיוון הזה נבחר בכוונה: אופסיידס נשאר עצמאי. הוא לא קורא לשום
מסד זר, ואם דוביד למטה — כלום לא נשבר.

```js
// אופסיידס, אחרי כניסה או במסך הפרסים
const { data: pending } = await supabase.rpc('pending_dubid_rewards');

for (const g of pending) {
  await grantSmokeGrenades(g.amount);      // הקוד שלך, הטבלאות שלך
  await supabase.rpc('claim_dubid_reward', { p_id: g.id });
}
```

### מה שאין כאן, בכוונה

אין פונקציה שמזכה יתרת 💨. דוביד אומר "מגיע לו"; **אופסיידס**
מחליט כמה זה שווה ומתי לזכות. שני מוצרים, גבול אחד.

`idempotency_key` הוא `UNIQUE` ברמת המסד. retry של cron או
לחיצה כפולה מחזירים את אותה הענקה בדיוק, לא שתיים. (נבדק.)

---

## 5. ★★ `public.server_now()` — לקרוא לפני שנוגעים ★★

**שני המוצרים קוראים לאותה פונקציה, כל אחד במסד שלו.**

| | קורא | מצפה ל |
|---|---|---|
| דוביד | `src/lib/serverTime.ts` | `bigint`, epoch ms |
| אופסיידס | `src/lib/serverTime.js` → `Number(data)` | epoch ms |

### הבור

גרסה מוקדמת של מיגרציה ניסתה "לקבע" אותה כ-`TIMESTAMPTZ`.
PostgreSQL עצר את זה:

```
ERROR: cannot change return type of existing function
```

אילו היה `DROP FUNCTION` לפני — זה היה עובר בשקט, ואופסיידס היה
מקבל `Number("2026-08-24 12:00:00+00")` = **NaN**. השעון היה
נשאר על היסט 0, בלי אף שגיאה, וכל ספירה לאחור — נעילת הימורים,
תפוגת boost, ריסט יומי — הייתה זזה לפי שעון המכשיר של המשתמש.

`01_dubid_bridge.sql` §4 רק **בודק**. אם מישהו יחליף את טיפוס
ההחזרה, הוא נכשל בהודעה שמסבירה בדיוק את זה.

**כלל: לא לגעת. אף פעם. משום צד.**

---

## 6. שיווק צולב — מה לקרוא בכתובת

דוביד שולח משתמשים עם שיוך מלא:

```
https://offsidebets.dubel.team/?ref=dubid&src=<placement>&v=<promo>&gw=<n>
```

| פרמטר | ערכים | מה זה אומר |
|---|---|---|
| `ref` | `dubid` | מאיזה מוצר |
| `src` | `lobby` · `locked` · `result` | מאיזה מסך |
| `v` | `just-locked` · `kickoff-soon` · `beaten` · `champion` · `idle` | איזה רגע |
| `gw` | מספר | איזה מחזור |

**אין בכתובת שום מזהה אישי.** יש בדיקה שמוודאת את זה
(`tests/growth.test.ts`). אל תוסיפו אחד.

מה שכדאי לעשות: לשמור את השלושה על הפרופיל בכניסה ראשונה
(העמודות כבר קיימות אחרי המיגרציה), ולהתאים את מסך הנחיתה.
משתמש שהגיע עם `v=beaten` בא עם הפסד טרי — כדאי שהמסך הראשון
יציע לו זירה שמתחילה בקרוב, לא הסבר על המשחק.

הצד של דוביד: `src/lib/growth.ts`. טהור, בלי DOM ובלי שעון, 16
בדיקות. הבחירה **דטרמיניסטית** — אותו הקשר נותן תמיד אותו מסר.

---

## 7. השם

השם באנגלית הוא **Offsides**. לא `Offside`, ולא `OFFSIDE BETS`
(שהופיע בתמונת הפרסומת עד עכשיו). בדוביד יש מקור אמת אחד:

```ts
// src/lib/growth.ts
export const OFFSIDES = {
  name: 'Offsides',
  nameHe: 'אופסיידס',
  url: 'https://offsidebets.dubel.team',
  domainLabel: 'OFFSIDEBETS.DUBEL.TEAM',
};
```

⚠ **הדומיין נשאר `offsidebets.dubel.team`** — זה המארח החי. אם
הוא אמור להשתנות לשם החדש, זו שורה אחת שם, ובדיקה אחת
(`tests/growth.test.ts`) תתפוס אם מישהו שכח.

---

## 8. דאטת כדורגל — הפתוח הגדול

**הבקשה:** להשתמש בדאטה שמגיע מה-API של אופסיידס ישירות, במקום
ששני המוצרים יסנכרנו פיקסצ׳רים בנפרד.

**זה נכון, וזה לא נבנה עדיין.** לא מהיסוס אלא מחוסר מידע: אני
לא יודע איך נראית הסכמה של אופסיידס. `matches`? `fixtures`? מה
המפתחות? האם יש `player_stats`?

הצד של דוביד מוכן לזה במלואו. הזרימה שכבר קיימת:

```
ספק → נורמליזציה → core.* → מנוע ניקוד → תוצאות משתמש
```

מה שחסר הוא ספק אחד נוסף בשכבת הנורמליזציה. כשיהיה לי דאמפ של
הסכמה — `pg_dump --schema-only` על `public` — זו עבודה של שעות,
לא ימים.

**עד אז:** דוביד מזין תוצאות ידנית דרך מסך הניהול (`/#admin`).
זה תוכנן ככה למחזור 2 בכוונה.

### מה שכן חשוב לשמור נקי בינתיים

גבולות. כל אחד מאלה צריך להישאר ישות מזוהה בשני הצדדים, גם
לפני שיש סנכרון: זהות · קבוצות · שחקנים · פיקסצ׳רים · מחזורים ·
אירועי משחק. **לא** למזג ניקוד — סולמות שונים, משחקים שונים.

---

## 9. מה לא לעשות

| ✗ | למה |
|---|---|
| לשנות את טיפוס ההחזרה של `server_now()` | שובר את שעון שני המוצרים בשקט |
| להריץ `db/08_offsides_coexistence.sql` | נכתב לפרויקט משותף שכבר לא קיים |
| להריץ את `supabase/migrations/000*.sql` שבריפו אופסיידס | הן של פרויקט אחר לגמרי (`cities`, `venues`, `bookings`) |
| לתת לדוביד לקרוא טבלאות של אופסיידס מהדפדפן | הגבול עובר בשרת. `auth` בלבד מהקליינט |
| לסמוך על טוקן אופסיידס בלי לאמת אותו מול אופסיידס | דפדפן יכול לשלוח כל דבר |
| למזג את הניקוד של שני המוצרים | סולמות שונים, משחקים שונים |

---

## 10. הצעה לפתיחת הצ׳אט הבא

> "אני ממשיך על אופסיידס. שני המוצרים עכשיו על פרויקטים נפרדים
> ומקושרים דרך `link-offsides`. תקרא את `docs/OFFSIDES-SYNC.md`
> ותעדכן את עצמך לפני שאתה עונה. אני צריך את הסכמה שלו
> (`pg_dump --schema-only --schema=public`) כדי לחבר את הדאטה."
