# לוג האירועים — הארכיטקטורה

> מסמך אחד שמסביר איך "מה קרה במגרש" הופך ל"כמה נקודות יש לך",
> ולמה זה בנוי כך ולא אחרת.

## הבעיה

לדוביד יש שני מצבי משחק. לכל מצב חוקים אחרים: גודל הרכב, תקציב,
מכפיל קפטן, מערכים מותרים. הפיתוי הטבעי הוא לתת לכל מצב לקרוא את
נתוני הכדורגל בעצמו ולחשב.

זה בדיוק המקום שבו מוצרים כאלה נשברים. שני מסלולי חישוב = שתי
אמיתות. יום אחד דוביד 5 יגיד שהשחקן כבש ודוביד 11 יגיד שלא, וכבר
לא תהיה דרך לדעת מי צודק.

## הפתרון

**מקור אמת אחד, כמה פרשנויות.**

```
ספק כדורגל
    ↓  מתאם (provider adapter)
┌──────────────────────────────┐
│      לוג האירועים            │  ← עובדות בלבד. append-only.
│  "שער · דקה 67 · שחקן X"     │     לא יודע מה זה ניקוד.
└──────────────┬───────────────┘
               │  project()   ← פעם אחת, לא פעם לכל מצב
               ▼
   PlayerPerformance / TeamOutcome
               │
       ┌───────┴────────┐
       ▼                ▼
   דוביד 5           דוביד 11      ← כל מצב מפרש לפי ה-RuleSet שלו
   (RuleSet)         (RuleSet)
```

הלוג לא יודע מה זה "דוביד 5". דוביד 5 לא יודע מה זה לוג.
`bus.ts` מחזיק את שניהם ולא מחזיק דעה משלו.

## הקבצים

| קובץ | תפקיד |
|---|---|
| `src/lib/events/types.ts` | אוצר המילים: מה יכול לקרות במשחק |
| `src/lib/events/log.ts` | הוספה, תיקון, סדר קנוני, טביעת אצבע |
| `src/lib/events/project.ts` | הקרנה לקלט של מנוע הניקוד הקיים |
| `src/lib/events/bus.ts` | שיגור לכל מצבי המשחק במקביל |
| `src/lib/scoring/ranking.ts` | שוברי שוויון ובונוס דיפרנציאל |

**מנוע הניקוד (`scoring/engine.ts`) לא שונה.** ההקרנה מחזירה בדיוק
את המבנים שהוא כבר מקבל. זו הייתה הדרישה, וזה מה שנעשה.

## שלושת הכללים

### 1. Append-only

אירוע שנרשם לא נמחק ולא משתנה. לעולם.

שער שנפסל בוואר לא נעלם מהלוג — נוספת שורת `Correction` שמצביעה
עליו. `effectiveEvents()` מסנן אותו מהחישוב, `auditTrail()` מציג
אותו לאדמין.

```ts
log = voidEvent(log, goalSeq, 'נפסל בוואר — עבירה בהתחלת המהלך');
```

הברִיף דורש "אין לשכתב ציונים היסטוריים בשקט". ככה זה נאכף.

### 2. סדר לפי `seq`, לא לפי שעון

`Date.now()` לא משתתף בשום החלטה. אותם אירועים באותו סדר מחזירים
תמיד את אותה תוצאה — גם בעוד שנה, גם בשרת אחר.

`fingerprint(log)` נותן חתימה יציבה. אם ציון של משתמש השתנה, אפשר
להוכיח שהנתונים השתנו ולא המנוע.

### 3. הקרנה אחת לכל המצבים

```ts
const projection = mergeProjections(logs.map((l) => project(l, roster)));
```

השורה הזו רצה **פעם אחת** ב-`broadcast()`. אם היא הייתה רצה פעמיים,
שני המצבים היו יכולים להיפרד. זה כל העניין.

## החלטות שכדאי להכיר

**ספיגות נספרות רק בזמן שהשחקן על המגרש.**
מגן שיצא בדקה 60 ב-0:0 לא מפסיד את השער הנקי בגלל שער בדקה 85.

**משחק שנזנח או נדחה לא מייצר תוצאה.**
"תיקו" למשחק שלא הסתיים הוא ניחוש שמחלק נקודות שאף אחד לא הרוויח.

**שער עצמי נזקף ליריבה** — במקום אחד בלבד, ב-`project.ts`.

**דיפרנציאל דורש ביצוע.**
בלי זה, המשחק האופטימלי הוא לבחור את השחקן הכי אנונימי בליגה
ולקטוף בונוס על כלום. `minPlayerPoints` סוגר את זה.

**מתחת ל-20 משתתפים הדיפרנציאל כבוי.**
עם 3 משתתפים כל שחקן הוא 33% — אחוזים חסרי משמעות.

## שוברי שוויון

לפי הסדר, ואף שלב לא מוסיף נקודות:

1. ניקוד כולל
2. ביצועי הקפטן
3. נקודות דיפרנציאל
4. שערים
5. בישולים
6. שערים נקיים
7. תרומה אישית נוספת
8. חותמת ההגשה המוקדמת

שוויון שנשאר גם אחרי כל השמונה מקבל **אותו מקום**, ומסומן `tied`.
המסך אומר מה שבר את השוויון — "הוכרע לפי הקפטן".

## מה עוד לא מחובר

הלוג בנוי ונבדק, אבל עדיין לא מוזן מספק אמיתי. כדי לחבר:

1. לכתוב מתאם שממיר תגובת API ל-`DraftEvent[]`.
2. לקרוא ל-`appendMany(log, drafts)`.
3. שאר המערכת כבר עובדת — `broadcast()` לא צריך לדעת מאיפה
   האירועים הגיעו.

**אין לפזר שמות שדות של הספק מחוץ למתאם.** הספק חייב להישאר
בר-החלפה.
