# דוביד · סבב עיצוב Q — הידוק המהדורה

> סבב קטן ומדויק: אפס פיצ׳רים חדשים, אפס שינויי התנהגות.
> רק סגירת הפערים האחרונים בין "שפת העיתון" לשאריות שפת
> האפליקציה הישנה, ושלושה תיקוני ליטוש שנמצאו בדרך.
>
> ✅ כל 353 הבדיקות עוברות · ✅ `vite build` עובר · נבדק חזותית
> בצילומי מסך של הלובי, הדירוג, הזירה והחוקים.

---

## קבצים שהשתנו (7)

| קובץ | מה |
|---|---|
| `index.html` | תמונת השיתוף בכתובת מוחלטת |
| `src/styles/index.css` | פס גלילה של המוצר · כיבוד "פחות תנועה" |
| `src/App.tsx` | בורר המצב בחוקים — אותו בורר כמו בדירוג |
| `src/components/AppShell.tsx` | אי־התאמת דפוס לכותרות כל המסכים |
| `src/components/Arena.tsx` | הזירה קיבלה כותרת מדור של עיתון |
| `src/components/Leaderboard.tsx` | הכרטיסים האחרונים בשפה הישנה הוחלפו |
| `src/components/Lobby.tsx` | משוב מגע למדורים · חץ שער · ניקוי |

פשוט להעתיק מעל הקיים ולפרוס כרגיל. אין מיגרציה, אין הגדרות.

---

## 1 · תמונת הקישור המשותף — תוקן באג אמיתי

`og:image` היה נתיב **יחסי** (`/brand/...`). וואטסאפ, טלגרם
ופייסבוק מושכים את התמונה מהשרתים שלהם — נתיב יחסי הוא
בשבילם כלום, והקישור ששותף יצא **בלי תמונה בכלל**.

זה הפריט שהכי הרבה אנשים רואים מהמוצר (כל שיתוף בקבוצת
וואטסאפ), ועכשיו הוא מציג את הלוגו כמו שצריך.

## 2 · הזירה הצטרפה לעיתון

הזירה הייתה המסך המרכזי היחיד שנפתח בלי כותרת עיתון — ישר
לתוך כרטיסים מעוגלים. עכשיו:

- כותרת מדור «זירת החברים» — קו כפול, סריף, פוליו — בדיוק
  כמו «דירוג חכמים».
- תיבת הפתיחה (כשאין עדיין זירות) ממוסגרת בקו כפול, כמו
  הודעה ממוסגרת בעמוד מודפס.

הטפסים, הקוד הגדול והכפתורים לא זזו — רק המסגרת סביבם.

## 3 · הדירוג — נשארו בו שני כרטיסים מהשפה הישנה

«התוצאה שלך» ורשימת «כבר בפנים» היו כרטיסי אפליקציה מעוגלים
מתחת לכותרת עיתון. עכשיו הם תיבות עימוד: «התוצאה שלך» בקו
כפול (הודעה חשובה), הרשימה בקו דק. אותו תוכן, אותו מיקום.

## 4 · בורר המצב בחוקים

בחוקים ישבו גלולות מעוגלות; בדירוג — מלבנים שמתמלאים בצבע
המצב. שני מסכים, אותה פעולה, שתי צורות. עכשיו יש אחת.

## 5 · הכותרת של כל מסך "הודפסה"

אי־התאמת לוחות הדפוס (הצל האדום־כחלחל של 0.7px) הייתה על
כותרות הלובי והדירוג אבל לא על כותרת המעטפת («ההרכב»,
«הזירה»…). עכשיו כל כותרת סריפית במוצר עברה באותו מכבש.

## 6 · פס הגלילה — חלק מהעמוד

בווינדוס, פס הגלילה של הדפדפן היה מלבן אפור־בהיר ברוחב 17px —
האלמנט הבהיר ביותר על מסך שכולו לילה וזהב. עכשיו: דק, שקוף,
זהב עמום בנגיעה. במובייל ממילא אין פס — זה נגע רק לדסקטופ,
בדיוק איפה שזה בלט.

## 7 · נגיעות קטנות

- **מדורי הלובי** («ליגות פרטיות», «הדירוג») מקבלים משוב
  לחיצה כמו הכתבות, וחץ זהוב קטן בפינה — הרמז שזה שער
  ולא תיבת מידע.
- **הכתבות בלובי** מתרוממות פיקסל בריחוף — בדסקטופ בלבד.
- **«פחות תנועה»**: גם אנימציית הברק (sheen) כבויה עכשיו למי
  שביקש. כפתור עובד גם בלי פס אור בלולאה.
- **ניקוי**: תנאי מת בתג המבזק של הלובי (`urgent ? אדום : אדום`).

---

## מה בכוונה **לא** נגעתי בו

- מסך בניית ההרכב והמגרש — עובדים, נעולים בבדיקות, ומושלמים.
- כרטיס השיתוף וכרטיס המנוי — קנבס, שפה סגורה.
- ניווט תחתון, מתג המצבים והטפסים — כרום אפליקציה במכוון;
  ההפרדה בין "עמוד מודפס" (תוכן) ל"כרום" (שליטה) היא הכלל,
  לא באג.
