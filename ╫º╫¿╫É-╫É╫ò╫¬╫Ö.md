# DUBID — סבב 4 · קבצים שהשתנו בלבד

מבנה תיקיות זהה. לפרוס מעל `Desktop\Dubid` ולאשר החלפה.

---

## ⚠ קובץ אחד צריך למחוק ידנית

```
db/schema.sql        ← למחוק
```

זיפ לא יכול לבטא מחיקה. **זה הקובץ שנתן לך את השגיאה**
`42710: type "i18n_name" already exists` — עותק ישן ולא־מוגן של
`db/01_schema.sql`. שני קבצים שעושים כמעט אותו דבר, ואחד מהם
שבור, זו מלכודת.

---

## חדשים

```
db/11_arena_and_squads.sql       זירות בשרת · עריכת סגלים · תקציב
db/tests/03_arena_squads.sql     11 בדיקות לכל מה שלמעלה
docs/INSTALL.md                  ★ מסמך ההתקנה המלא
docs/CAMPAIGN-OFFSIDES.md        תוכנית הקמפיין
src/components/OffsidesAds.tsx   שלוש יחידות פרסום
src/components/AdminSquads.tsx   מסך עריכת הסגלים
```

## שהשתנו

```
db/03_seed_squads.sql            ★ מזין fantasy_price — היה חסר
index.html                       Google Analytics
src/lib/leagueStore.ts           ★ localStorage → Supabase
src/lib/growth.ts                מקומות, רמות פרסום, טקסטים
src/lib/store.ts                 פונקציות אדמין לסגלים
src/components/Arena.tsx         כתיבות אסינכרוניות + פרסום
src/components/AdminPanel.tsx    לשונית "סגלים ושחקנים"
src/components/Leaderboard.tsx   פרסום מתחת לטבלה
src/components/LockedLineup.tsx  שורת פרסום במצב ההמתנה
src/App.tsx                      כרזה בכרטיס, שורות בחוקים
preview/lobby.html               תצוגה מקדימה
```

---

## סדר הרצה

```sql
-- 1. הקבצים החדשים
db/11_arena_and_squads.sql

-- 2. שוב, כדי להזין את המחירים שהיו חסרים (אידמפוטנטי)
db/03_seed_squads.sql

-- 3. הבדיקה שקובעת
SELECT * FROM game.v_arena_health;   -- players_without_price = 0
```

**הכל מוסבר צעד־צעד ב-`docs/INSTALL.md`.**
