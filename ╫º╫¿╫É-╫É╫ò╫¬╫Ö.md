# DUBID — סבב 5 · 10 קבצים

לפרוס מעל `Desktop\Dubid` ולאשר החלפה.

---

# 🔑 האדמין

```
https://dubid.dubelteam.com/admin        ← בייצור
http://localhost:5173/admin              ← בפיתוח
```

**סיסמה:** `hapoelTA14!`

`#admin` הישן ממשיך לעבוד.

---

## סדר הרצה — שתי שורות ב-SQL Editor

```
db/11_arena_and_squads.sql     ← שוב. מכיל תיקון (short_code)
db/12_admin_access.sql         ← חדש. הסיסמה
```

שניהם אידמפוטנטיים.

---

## ואז — "בדיקת מערכת"

`/admin` → לשונית **"בדיקת מערכת"**.

היא בודקת חמש חוליות בסדר התלות ואומרת בדיוק מה חסר. **זה מה
שיפתור לך את "אין חיבור לשרת"** — כמעט בוודאות `db/09` ואילך
עוד לא רצו אצלך.

---

## הקבצים

### חדשים
```
db/12_admin_access.sql          כניסת אדמין בסיסמה אחת
db/tests/04_admin_access.sql    6 בדיקות — כולל נעילה
```

### שהשתנו
```
db/11_arena_and_squads.sql      ★ תיקון short_code → מסך הסגלים נשבר בלעדיו
db/tests/03_arena_squads.sql    הורחב — הפונקציה באמת רצה עכשיו
src/lib/store.ts                ★ אבחון שגיאות + claim_admin + בדיקות מערכת
src/App.tsx                     ★ נתיב /admin + פס שאומר מה חסר
src/components/AdminPanel.tsx   מסך כניסה חדש + לשונית בדיקות
index.html                      Google Analytics (G-F75FRV6MMR)
docs/INSTALL.md                 שלב 3 נכתב מחדש
```

**הכל מוסבר ב-`מה-השתנה.md`.**
