# DUBID — סבב 6 · תיקון שתי התקלות

לפרוס מעל `Desktop\Dubid` ולאשר החלפה.

---

## 1. `42P16: cannot drop columns from view`

**שבעה קובצי SQL תוקנו.** כל תצוגה עושה עכשיו `DROP` לפני
`CREATE`, ולכן אפשר להריץ את המיגרציות **בכל סדר וכמה פעמים
שרוצים**. קודם זה לא היה נכון, למרות שכתבתי שכן.

▸ להריץ שוב:

```
db/09_live_mvp.sql
db/10_accounts.sql
db/11_arena_and_squads.sql
db/12_admin_access.sql
```

## 2. 404 על `/admin`

**`/admin` הוא עכשיו קובץ אמיתי** ולא הגדרת שרת:

```
public/admin.html
public/admin/index.html
```

▸ **חייב build + פריסה מחדש** כדי שהקבצים יגיעו ל-`dist/`.

```bash
npm run build
```

---

# 🔑 אחרי הפריסה

```
https://dubid.dubelteam.com/admin
```

**סיסמה:** `hapoelTA14!`

→ לשונית **"בדיקת מערכת"** תגיד לך מה עוד חסר.

---

## הקבצים

```
db/01_schema.sql                    DROP לפני CREATE בתצוגות
db/08_offsides_coexistence.sql      "
db/09_live_mvp.sql                  "
db/10_accounts.sql                  ★ זה שנפל
db/11_arena_and_squads.sql          "
db/12_admin_access.sql              "
db/offsides/01_dubid_bridge.sql     "
public/admin.html                   ★ חדש — /admin
public/admin/index.html             ★ חדש — גיבוי
vercel.json                         rewrite כגיבוי
docs/INSTALL.md                     פתרון תקלות
```
