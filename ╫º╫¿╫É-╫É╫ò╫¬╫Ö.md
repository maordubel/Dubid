# DUBID — סבב 8 · 5 קבצים

לפרוס מעל `Desktop\Dubid`, ואז:

```bash
npm run build
```

ולפרוס. ואז `/admin` עם `hapoelTA14!`.

---

## מה תוקן

**שני התסמינים שדיווחת — נכנס בלי סיסמה, ולא מאפשר פעולות —
היו אותו באג.**

היו שני מקורות אמת ל"אני אדמין": דגל בדפדפן, ו-`is_admin`
במסד. הסיסמה הדליקה את הדגל המקומי, הקריאה לשרת נכשלה, ונכנסת
למסך שאי אפשר לפעול בו — ומאז דילגת על מסך הכניסה בכל רענון.

**עכשיו יש מקור אחד: המסד.** הדגל המקומי נמחק לגמרי.

## הקבצים

```
src/lib/adminGate.ts            ★ חדש — מכונת המצבים, טהורה
tests/adminGate.test.ts         ★ חדש — 5 בדיקות
src/lib/store.ts                ★ נמחק ממנו כל שער הסיסמה המקומי
src/components/AdminPanel.tsx   ★ שער אחד + דלת חירום
```

## אם עדיין לא נכנס

המסך יגיד לך למה. ואם המסד לא מכיר את הפונקציה — הוא יציג את
**המזהה שלך ואת השורה המדויקת** להדבקה ב-SQL Editor, עם כפתור
העתקה. אין מצב שבו נשארת בלי דרך פנימה.
