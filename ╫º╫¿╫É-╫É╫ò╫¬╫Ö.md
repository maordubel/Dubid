# דוביד · הכל און ליין

חבילת דלתא. **רק** הקבצים שנוצרו או שונו בסבב הזה — לא כל
הפרויקט. פרוס אותם מעל התיקייה הקיימת, לפי מבנה התיקיות.

---

## למה זה נשלח

אמרת: *"הכניסה לאדמין עובדת, אבל זה לא שומר ומשנה באמת את
הנתונים."*

צדקת בחצי. הכתיבה למסד הצליחה — **הקריאה מעולם לא קרתה.**
הקבוצות, השחקנים והלוח ישבו בקובץ TypeScript שנארז לתוך
ה‑JavaScript בזמן בנייה, ולכן שום שינוי באדמין לא יכול היה
להופיע על מסך. ההסבר המלא: `docs/ONLINE.md`.

---

## שלושה שלבים

### 1 · המסד

Supabase → SQL Editor → הדבק והרץ את **`db/RUN-ALL.sql`**.

אידמפוטנטי — אפשר להריץ שוב בלי לשבור כלום. אם כבר הרצת את
המיגרציות הקודמות, מספיק **`db/13_live_data.sql`**.

בדיקה:

```sql
SELECT * FROM game.v_live_health;
```

`players_without_price` ו‑`players_without_tier` חייבים להיות **0**.

### 2 · Realtime

Supabase → Database → Replication → ודא ש‑**`game.data_revision`**
מסומנת ב‑`supabase_realtime`.

זה מה שגורם לשינוי אצלך לקפוץ למסכים של אחרים בלי רענון.

### 3 · הפרונט

```bash
npm install
npm run build
```

ופרוס. **חובה** — יש כאן קבצים חדשים, ובלי בנייה הדפדפן ימשיך
להריץ את הגרסה הישנה.

---

## איך יודעים שזה עבד

לוח ניהול → לשונית **מערכת**. שתי בדיקות חדשות:

- **דאטת כדורגל מהשרת** — צריכה להראות `14 קבוצות · 351 שחקנים`
- **טיוטות בשרת** — ההרכב שבבנייה עובר בין מכשירים

ובתחתית לשונית **תוצאות** צריך להופיע `· מהשרת` בזהב.
אם מופיע `· מהקובץ המקומי — אין חיבור` באדום, שלב 1 או 2 לא
הושלמו.

---

## מה בחבילה

```
db/13_live_data.sql            המיגרציה החדשה
db/RUN-ALL.sql                 כל המיגרציות בקובץ אחד (נבנה מחדש)
db/tests/05_live_data.sql      11 בדיקות למעגל הסגור
scripts/build-all-sql.mjs      מכיר עכשיו את 13

src/lib/liveData.ts            חדש · הרישום החי של סגלים ולוח
src/lib/drafts.ts              חדש · טיוטות מול השרת
src/lib/draftWire.ts           חדש · התרגום מסך↔מסד (טהור, נבדק)

src/App.tsx                    טוען דאטה חיה; דדליין מהשרת
src/main.tsx                   מנקה שאריות מקומיות
src/data/fixtures.ts           הלוח והדדליין הפכו לקישורים חיים
src/lib/store.ts               בלי מטמון מקומי; RPC ניהול חדשים
src/lib/identity.ts            השם חי בשרת בלבד
src/lib/leagueStore.ts         בלי מטמון מקומי
src/state/useLineup.ts         טיוטה נשמרת לשרת בהשהיה
src/components/AdminPanel.tsx  לשונית "מחזור" + חיווי מקור הדאטה
src/components/AdminSquads.tsx דרג לשחקן חדש; הטקסט תוקן

tests/purge.test.ts            נכתב מחדש לחוזה החדש
tests/draftWire.test.ts        חדש · 8 בדיקות
tests/liveData.test.ts         חדש · 14 בדיקות

public/sw.js                   ★ תיקון ה-Service Worker
public/reset.html              ★ דלת מילוט למי שנתקע
vite.config.ts                 ★ מטביע מזהה בנייה ב-sw.js
index.html                     ★ שומר סגנונות שמרפא את עצמו

docs/ONLINE.md                 ההסבר המלא
```

★ = ארבעת הקבצים שמתקנים את התקלה מהסבב הקודם, שבה האתר עלה
בלי עיצוב. הם היו מוכנים ולא נשלחו. פירוט בתוך הקבצים עצמם.

---

## מה נבדק

על PostgreSQL 16 אמיתי ובקוד:

- **48 בדיקות SQL** — על מסד ריק, פעמיים ברצף
- **251 בדיקות יחידה**
- `tsc --noEmit` נקי
- רינדור מלא של האפליקציה ושל לוח הניהול

## מה לא נבדק

`npm run build`. אין גישה ל‑registry מהסביבה שבה עבדתי — זה
החלק היחיד שאני מבקש שתריץ ותגיד לי אם נפל. גם ה‑Edge Functions,
כניסת גוגל, וקישור חשבון אופסיידס לא נבדקו כאן, מאותה סיבה.

API‑Football עדיין לא מחובר, לפי בקשתך.
