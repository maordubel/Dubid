# DUBID — סבב 7

לפרוס מעל `Desktop\Dubid` ולאשר החלפה.

---

# ⚡ ארבעה שלבים ואתה משחק

## 1 · המסד — הדבקה אחת

Supabase → SQL Editor → New query → להדביק את **כל**
`db/RUN-ALL.sql` → **Run**.

אחד־עשר הקבצים, בסדר הנכון, בפעם אחת. אפשר להריץ שוב כמה
פעמים שרוצים.

✔ בסוף: `✓ הושלם.  קבוצות 14 · שחקנים 351 · משחקים 7`

## 2 · שני מתגים

```
Settings → API → Exposed schemas:
    public, core, game, shared

Authentication → Providers → Anonymous sign-ins:  ON
```

⚠ **בלי שני אלה המסד מוכן והאפליקציה לא תתחבר.** זה הגורם הכי
נפוץ ל"אין חיבור לשרת".

## 3 · build

```bash
npm run build
```

חובה — `/admin` הוא קובץ חדש.

## 4 · כניסה

```
https://dubid.dubelteam.com/admin       סיסמה: hapoelTA14!
```

שגיאה? → **"בדיקת מערכת — מה חסר בשרת"** במסך הכניסה עצמו.

---

# ואז לשחק

```sql
UPDATE game.gameweeks SET status = 'open' WHERE code = 'gw-2';
```

ולשלוח לחברים: `https://dubid.dubelteam.com`
**בלי הרשמה.** נכנסים, בוחרים, מגישים.

📄 הכל ב-**`docs/GO-LIVE.md`**.

---

## הקבצים

```
db/RUN-ALL.sql                   ★ חדש — כל המיגרציות בקובץ אחד
scripts/build-all-sql.mjs        ★ חדש — מה שבונה אותו
docs/GO-LIVE.md                  ★ חדש — ארבעה שלבים
src/lib/store.ts                 ★ תיקון — "השרת לא ענה" הסתיר את הסיבה
src/components/AdminPanel.tsx    ★ בדיקת מערכת ממסך הכניסה
docs/INSTALL.md                  המדריך המלא
```
