# DUBID — סבב 3 · קבצים שהשתנו בלבד

התיקייה בנויה **באותו מבנה** של הפרויקט. לפרוס מעל
`Desktop\Dubid` ולאשר החלפה — כל קובץ נוחת במקומו.

הפירוט המלא של מה השתנה ולמה: `מה-השתנה.md` שבשורש.

---

## מה יש כאן

### חדשים

```
db/10_accounts.sql                  חשבונות: הרשמה, אורחים, שמות משתמש
db/tests/02_accounts.sql            8 בדיקות — כולל השחזור של הבאג מ-§8
docs/AUTH.md                        המדריך המלא להרשמה ולהגדרות
src/components/AuthPanel.tsx        טופס גוגל / אימייל
src/components/RegisterNudge.tsx    ההצעה להירשם (הציור)
src/lib/nudge.ts                    ההצעה להירשם (הכלל, נבדק)
tests/nudge.test.ts                 6 בדיקות לכלל
public/brand/dubel-team*.png        הלוגו של דובל טים
public/brand/offsides*.png          הלוגו של אופסיידס
brand/source/*.png                  המקורות שמהם נגזרו
```

### שהשתנו

```
src/lib/growth.ts                   ★ שלושת הדומיינים — מקור אמת אחד
src/lib/identity.ts                 + הרשמה, גוגל, יציאה
src/components/LockedLineup.tsx     ★ נבנה מחדש על אותו מגרש כמו מסך הבחירה
src/components/OffsidesBanner.tsx   הלוגו האמיתי במקום הדגל המצויר
src/components/DubelCredit.tsx      הלוגו האמיתי + הדומיין מהמקור המשותף
src/components/AccountSheet.tsx     ההרשמה נכנסה ללשונית "החשבון"
src/components/Lobby.tsx            משבצת להצעת ההרשמה
src/App.tsx                         חיווט + תיקון `fill` במסך ההרכב
db/tests/00_supabase_shim.sql       + is_anonymous, raw_user_meta_data
db/tests/01_live_flow.sql           ניקוי בתחילת ריצה — אפשר להריץ שוב
db/offsides/01_dubid_bridge.sql     דומיין
docs/SUPABASE.md · docs/OFFSIDES-SYNC.md
tests/growth.test.ts                הדומיין החדש
preview/lobby.html                  תצוגה מקדימה — פותחים בדפדפן
```

---

## סדר הרצה

1. `npm run dev` — הקוד עובד כמו שהוא.
2. **`db/10_accounts.sql`** על פרויקט דוביד (אחרי 09), ואז:
   ```sql
   SELECT * FROM game.v_accounts_health;   -- duplicate_usernames = 0
   ```
3. **Anonymous sign-ins חייב להיות דלוק** — בלעדיו אין אורחים.
4. שאר ההגדרות (גוגל, Site URL, אישור מייל): `docs/AUTH.md`.
