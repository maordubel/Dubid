# דוביד ⚽

משחק הרכב שבועי על ליגת העל. **בלי הרשמה** — נכנסים ומשחקים.

---

## מאיפה מתחילים

👉 **`מה-לעשות.md`** — כל מה שצריך ממך, לפי סדר. שתי דקות.

---

## מה יש כאן

```
db/            כל המסד. RUN-ALL.sql = הכל בקובץ אחד.
  tests/       11 חבילות בדיקה שרצות על PostgreSQL אמיתי
src/           האפליקציה (React + TypeScript)
  components/  מסכים
  lib/         לוגיקה — ניקוד, זהות, כרטיס, פרסום
  data/        סגלים, קבוצות, לוח משחקים
public/        לוגו, סמלים, אייקונים
docs/          תיעוד לפי נושא
tests/         353 בדיקות יחידה
supabase/      פונקציות קצה — **אופציונליות**, לא נדרשות
scripts/       כלי בנייה (בניית RUN-ALL, סגלים)
```

## הרצה מקומית

```bash
npm install
npm run dev
```

★ ברירת המחדל מצביעה לפרויקט **הייצור**. לפיתוח:
`cp .env.staging.example .env.local` (ראו את הקובץ עצמו).

## בדיקות

```bash
npm test              # 353 בדיקות יחידה
npx tsc --noEmit      # טיפוסים
```

## תיעוד לפי נושא

| קובץ | על מה |
|---|---|
| `docs/GUEST-PASS.md` | זהות אורח, כרטיס המנוי, לכידת לידים |
| `docs/PHASE-1-2-STATUS.md` | מצב הפרודקשן והאבטחה, פריט-פריט |
| `docs/ADMIN.md` | לוח הניהול |
| `docs/SUPABASE.md` | המסד |
| `docs/OFFSIDES-SYNC.md` | הגבולות מול אופסיידס |

---

**אין צורך ב-Supabase CLI ואין צורך לפרוס פונקציות.**
כל המוצר רץ מ-`db/RUN-ALL.sql` + הקוד.
